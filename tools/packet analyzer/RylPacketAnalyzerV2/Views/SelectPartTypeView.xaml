<UserControl x:Class="RylPacketAnalyzerV2.Views.SelectPartTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="37" />
            <RowDefinition Height="37" />
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center" Grid.Row="0" Text="Type" FontWeight="Bold" />
        <ComboBox Grid.Row="1" Height="25" Margin="6" ItemsSource="{Binding Types}" SelectedValuePath="Value" SelectedValue="{Binding SelectedType}" DisplayMemberPath="Key"/>
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="Ok" Content="Ok" Width="100" Height="25" Margin="6" />
            <Button x:Name="Cancel" Content="Cancel" Width="100" Height="25" Margin="6"/>
        </WrapPanel>
    </Grid>
</UserControl>
